RequestCounterFilter
LoginServlet

// 전처리(요청이 요청을 처리할 서블릿으로 가기 전)
chain.doFilter
// 후처리(생성된 응답이 클라이언트에게 가는 중)

// 전처리 ==> 서블릿 ==> 후처리 

1. logger.debug("RequestCounterFilter 전 처리 부분-chain.doFilter 호출 전");
2. logger.debug("loginServlet doGet");
3. logger.debug("RequestCounterFilter 후 처리 부분-chain.doFilter 호출 후");
